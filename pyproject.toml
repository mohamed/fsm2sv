[project]
name = "fsm2sv"
version = "1.0"
description = "Generate synthesizable SystemVerilog FSMs from YAML specifications"
requires-python = ">=3.8"
dependencies = [
    "jsonschema>=4.0",
    "pyyaml",
    "ruff>=0.15.12",
]